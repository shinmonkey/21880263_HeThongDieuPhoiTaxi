@model IEnumerable<ServerService.Models.KhachHang>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KhGpsLon)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KhGpsLat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kh)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.KhGpsLon)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KhGpsLat)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kh.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.KhId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.KhId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.KhId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

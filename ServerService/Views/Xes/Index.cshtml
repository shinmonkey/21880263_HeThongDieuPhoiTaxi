@model IEnumerable<ServerService.Models.Xe>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.XeBienso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.XeGiayDangky)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lx)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tx)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.XeBienso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.XeGiayDangky)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lx.LxId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tx.TxId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.XeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.XeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.XeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
